(function($) {

    window.render = {

        renderAll: function () {
            window.render.banner();
        },

        banner: function() {
            $('.main-banner').html(
                `<div class="banner header horizontal-bar sticky">
                    <a id="title" href='/'>🏈 Tackle Fatherhood 🏈</a>
                    <a class="bar-option" href="/position">Position</a>
                    <a class="bar-option" href="/huddle">Huddle</a>
                    <a class="bar-option" href='/playbook'>Playbook</a>
                </div>`
            );
        }

    };

})(jQuery);

jQuery(window.render.renderAll);

$(document).on('turbolinks:load', window.render.renderAll);